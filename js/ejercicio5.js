//Dado un arreglo de strings, retorna cuántos elementos tiene el arreglo.

function conElem(arreglo) {
    return arreglo.length; // Retorna la cantidad de elementos
}

let palabras = ["hola", "mundo", "javascript", "es", "genial", "tengo", "sueño"];
let cantidad = conElem(palabras);
console.log(cantidad); 
