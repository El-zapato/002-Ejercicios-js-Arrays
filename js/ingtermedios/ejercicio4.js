//Dado un arreglo de palabras, únelas en una sola cadena separada por comas.

const palabras = ["skibidi", "pomni", "sigma", "cuchara", "arroz"];
const resultado = palabras.join(", "); //join une todos los elementos en una sola cadena

console.log(resultado);
